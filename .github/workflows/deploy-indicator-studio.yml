name: Deploy Indicator Studio

on:
  push:
    branches:
      - main
    paths:
      - "BA 量化/ai-quant-lab/indicator-studio/**"
      - ".github/workflows/deploy-indicator-studio.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: "BA 量化/ai-quant-lab/indicator-studio"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
